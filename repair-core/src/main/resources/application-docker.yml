# Docker profile overrides — service names instead of localhost
spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/repair_db
  kafka:
    bootstrap-servers: kafka:9092
  data:
    redis:
      host: redis
